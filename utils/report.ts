import LoggerSDK from './logger-sdk';

const getLogInfoFromTarget = (target) => {
}


const addReportListener = (el: HTMLElement) => {
  const handleClick = (eventName, data) => {
    report(eventName, data);
    // 上报
  }

  el.addEventListener('click', (e) => {
    
    if () {
    
    }
  });
}

addReportListener();